{
  "name": "meridian",
  "version": "1.0.0",
  "private": true,
  "description": "Meridian — a full-stack multi-category e-commerce marketplace (MERN)",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "db": "npm run dev:db --prefix server",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently -n mongo,api,web -c green,blue,magenta \"npm run dev:db --prefix server\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "seed": "npm run seed --prefix server",
    "test": "npm test --prefix server",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
